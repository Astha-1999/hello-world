num=int(input("enter the number\n"))
for i in range(num):
    temp=i
    sum = 0
    while temp > 0:
        digit = temp %10
        sum = sum + (digit ** 3)
        temp = temp //10
    if i==sum:
        print (i)
        i=i + 1
